{
  "name": "Senior AI Video Dev",
  "dockerFile": "Dockerfile",
  "context": "..",
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "postCreateCommand": "cd backend && npm install || true && cd ../agent && python3 -m venv .venv || true && . .venv/bin/activate && pip install -r requirements.txt || true",
  "forwardPorts": [5000, 3000],
  "extensions": [
    "ms-azuretools.vscode-docker",
    "ms-vscode-remote.remote-wsl"
  ]
}
